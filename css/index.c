#include <kesalahan.h>

int hati() {
    bool diMaafkan;
    bool pemerintah;

    if (diMaafkan && !pemerintah) {
        printf("Minal 'Aidin wal Faizin");
        free(kesalahan);
    } else {
        printf("Aja sendiri");
    }

    return THR;
}